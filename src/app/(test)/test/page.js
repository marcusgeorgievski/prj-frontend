import { ComboboxFormItem } from '@/components/notes/class-combobox';

export default function TestPage() {
  return (
    <div className="flex items-center justify-center h-screen gap-6">
      <ComboboxFormItem form={{}} />
    </div>
  );
}
